!! Generated by rev-ce-builder for {{ hostname }} ({{ serial_number }})
!! Scenario: {{ scenario_key }}  Site: {{ site_code }}
!
hostname {{ hostname }}
!
banner motd ^
  {{ hostname }} -- managed by rev-ce-builder. Authorized access only.
^
!
{# --------------------- placeholder; user will provide template --------------------- #}
{# Replace this block with the production IOS-XR template you mentioned.            #}
interface Loopback0
 description Router-ID / iBGP source
 ipv4 address {{ loopback0 }} 255.255.255.255
!
{% for intf in interfaces %}
interface {{ intf.name }}
 description {{ intf.description }}
{% if intf.get('ipv4_address') %}
 ipv4 address {{ intf.ipv4_address }} {{ intf.ipv4_netmask }}
{% endif %}
{% if intf.get('ipv6_address') %}
 ipv6 address {{ intf.ipv6_address }}/{{ intf.ipv6_prefix_length }}
{% endif %}
 no shutdown
!
{% endfor %}
!
end
